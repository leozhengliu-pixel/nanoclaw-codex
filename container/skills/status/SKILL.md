# status

Use the host control plane to inspect recent runtime state.

- Prefer listing recent tasks and remote control events before guessing.
- Summarize whether auth, groups, and scheduler state look healthy.
