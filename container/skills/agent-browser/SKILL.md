# agent-browser

Use browser-style reasoning when a task needs online context, but keep execution inside the host tool boundary.

- Ask the host for fetched content or summaries instead of assuming raw network access from inside the container.
