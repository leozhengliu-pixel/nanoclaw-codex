# capabilities

Explain the current host and runtime capabilities exposed to the agent.

- Mention available scheduling modes: `once`, `interval`, and `cron`.
- Mention that group mounts and outbound messages must go through host tools.
